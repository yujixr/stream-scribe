# Stream Scribe Settings
#
# このファイルでデフォルト設定を上書きできます。
# 設定項目の詳細は stream_scribe/domain/settings.py を参照してください。
#
# 設定の優先順位（後勝ち）:
#   1. デフォルト値（stream_scribe/domain/settings.py）
#   2. このファイル（config.toml）
#   3. config.local.toml（存在する場合）

[summary]
# LLMバックエンド: "claude" または "vllm"
# backend = "claude"

# Claude設定（backend = "claude" の場合）
# anthropic_api_key = "sk-ant-..."
# claude_model = "claude-haiku-4-5-20251001"

# vLLM設定（backend = "vllm" の場合）
# vllm_base_url = "http://localhost:8000/v1"
# vllm_model = "Qwen/Qwen3-30B-A3B"
